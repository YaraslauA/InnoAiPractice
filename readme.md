We are writing an html page with javascript. This page should have a table that users can fill with monthly expenses. Monthly expenses consist of the name and the amount of money spent on them. The page should also feature the `calculate` button. After button presses we should calculate the total expenses, display the top 3 largest expenses and show average daily expenses. Average daily expenses should be calculated by dividing the total expenses by 30. Can you create a code for such a page?

We need to write test scenarios for the API that we can run in the Postman. The API endpoint link is `https://fakestoreapi.com/products`. This test data contains a list of the products in the form of the JSON file. We need to make sure that the API returns 200 responses. We need to validate each position for presence of the field ‘title’, field ‘price’ and filed ‘rating.rate’ being less than five. All not valid items should be put in the separate list.

We have an SQLite table named “orders”. Table have TEXT filed ‘customer’, REAL field ‘amount’ and DATE filed ‘order_date’. We need to create 3 separate SQL queries. The first query should collect the total amount for March. The second should find the top spending customer of all time. The third query should find average order value
